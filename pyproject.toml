[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agentguard-local"
version = "0.1.0"
description = "Controlled identity-session, browser lifecycle, timer, observer, and policy guardrails for user-owned AI agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
agentguard = "agentguard.cli:main"

[tool.setuptools]
packages = ["agentguard", "adapters"]

[tool.pytest.ini_options]
testpaths = ["tests"]
